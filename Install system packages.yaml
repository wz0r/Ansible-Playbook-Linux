- hosts: servers
  tasks:
    - name: Install required system packages
      apt:
        pkg:
          - htop
          - lsb-core
        state: latest
        force_apt_get: yes
        update_cache: true
