- hosts: servers
  tasks:
    - name: Install required system packages
      apt:
        pkg:
          - htop
          - lsb-core
          - 7zip
        state: latest
        force_apt_get: yes
        update_cache: true
