- hosts: servers
  tasks:
    - name: Install required system packages
      apt:
        pkg:
          - apt-lsb-core
          - apt-htop
        state: latest
        force_apt_get: yes
        update_cache: true
